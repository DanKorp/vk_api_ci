const loginForm = require('./loginForm');
const vkIdForm = require('./vkIdForm');
const sidebarMenuForm = require('./sidebarMenuForm');
const wallForm = require('./wallForm');
const topBarForm = require('./topBarForm');
const bottomBarLoginForm = require('./bottomBarLoginForm');

module.exports = {
    loginForm,
    vkIdForm,
    sidebarMenuForm,
    wallForm,
    topBarForm,
    bottomBarLoginForm
}